// Declare and use an array, and iterate over its elements using a loop

void main()
{
    int[] numbers = { 1, 2, 3, 4, 5 };

    for (uint i = 0; i < numbers.length(); i++)
    {
        print("Number at index " + i + " is: " + numbers[i]);
    }
}
